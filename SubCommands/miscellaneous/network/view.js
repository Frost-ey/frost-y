const { SlashCommandBuilder } = require(`@discordjs/builders`);
const { execute } = require("../../../Commands/miscellaneous/network");

module.exports = {
    name: "view",
    mainCmd: "network",
    async execute(interaction) {
        const 
    }
}